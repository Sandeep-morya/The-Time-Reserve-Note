{
	"name": "hardhat-project",
	"scripts": {
		"clean": "hardhat clean",
		"test": "hardhat test",
		"start": "hardhat node",
		"compile": "hardhat compile",
		"build": "hardhat run scripts/deploy.js --network sepolia",
		"verify": "hardhat verify --network sepolia"
	},
	"devDependencies": {
		"@nomicfoundation/hardhat-toolbox": "^4.0.0",
		"hardhat": "^2.19.4"
	},
	"dependencies": {
		"@openzeppelin/contracts": "^5.0.1",
		"dotenv": "^16.3.1",
		"react-icons": "^5.0.0",
		"react-router-dom": "^6.21.1"
	}
}
